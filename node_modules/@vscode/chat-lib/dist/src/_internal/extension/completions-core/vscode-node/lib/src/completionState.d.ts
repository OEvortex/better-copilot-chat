import { Position, ProposedTextEdit, TextEdit } from '../../types/src';
import { IntelliSenseInsertion, ITextDocument, type TextDocumentContents } from './textDocument';
export declare class CompletionState {
    private readonly _textDocument;
    private readonly _position;
    readonly originalPosition: Position;
    readonly originalVersion: number;
    readonly originalOffset: number;
    private readonly _editsWithPosition;
    constructor(_textDocument: ITextDocument, _position: Position, edits?: ProposedTextEdit[], originalPosition?: Position, originalVersion?: number, originalOffset?: number);
    get textDocument(): TextDocumentContents;
    get position(): Position;
    get editsWithPosition(): ProposedTextEdit[];
    private updateState;
    updatePosition(position: Position): CompletionState;
    addSelectedCompletionInfo(selectedCompletionInfo: IntelliSenseInsertion): CompletionState;
    applyEdits(edits: TextEdit[], isSelectedCompletionInfo?: boolean): CompletionState;
}
export declare function createCompletionState(textDocument: ITextDocument, position: Position): CompletionState;
//# sourceMappingURL=completionState.d.ts.map