import { ICompletionsTextDocumentManagerService } from './textDocumentManager';
/**
 * A tracker which can take an arbitrary number of actions to run after a given timeout
 * When all pushed timeouts have been resolved, the tracker disposes of itself.
 */
export declare class ChangeTracker {
    private _offset;
    get offset(): number;
    private _referenceCount;
    private _tracker;
    private _isDisposed;
    constructor(fileURI: string, insertionOffset: number, documentManager: ICompletionsTextDocumentManagerService);
    push(action: () => void, timeout: number): void;
}
//# sourceMappingURL=changeTracker.d.ts.map