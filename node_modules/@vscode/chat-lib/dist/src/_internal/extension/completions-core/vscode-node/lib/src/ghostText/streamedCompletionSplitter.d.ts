import { IInstantiationService } from '../../../../../../util/vs/platform/instantiation/common/instantiation';
import { FinishedCallback } from '../openai/fetch';
import { APIChoice } from '../openai/openai';
export declare class StreamedCompletionSplitter {
    private readonly prefix;
    private readonly languageId;
    private readonly initialSingleLine;
    private readonly trimmerLookahead;
    private readonly cacheFunction;
    private readonly instantiationService;
    private readonly lineLimit;
    private readonly completions;
    constructor(prefix: string, languageId: string, initialSingleLine: boolean, trimmerLookahead: number, cacheFunction: (prefixAddition: string, item: APIChoice) => void, instantiationService: IInstantiationService);
    getFinishedCallback(): FinishedCallback;
    private getCompletion;
    private trimOnce;
    private trimAll;
    private trim;
    private cacheCompletion;
    private adjustedAnnotations;
}
//# sourceMappingURL=streamedCompletionSplitter.d.ts.map