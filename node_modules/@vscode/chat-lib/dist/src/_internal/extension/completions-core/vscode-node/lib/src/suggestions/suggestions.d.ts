import { ServicesAccessor } from '../../../../../../util/vs/platform/instantiation/common/instantiation';
import { Logger } from '../logger';
import { APIChoice } from '../openai/openai';
import { IPosition, TextDocumentContents } from '../textDocument';
export interface ILines {
    getLineText(lineIdx: number): string;
    getLineCount(): number;
}
export declare function maybeSnipCompletionImpl(doc: ILines, position: IPosition, completion: string, blockCloseToken: string): string;
/**
 * Post-processed a completion choice in the context of the document where the choice is offered.
 */
export declare function postProcessChoiceInContext(accessor: ServicesAccessor, document: TextDocumentContents, position: IPosition, choice: APIChoice, isMoreMultiline: boolean, logger: Logger): APIChoice | undefined;
export declare function checkSuffix(document: TextDocumentContents, position: IPosition, choice: APIChoice): number;
//# sourceMappingURL=suggestions.d.ts.map