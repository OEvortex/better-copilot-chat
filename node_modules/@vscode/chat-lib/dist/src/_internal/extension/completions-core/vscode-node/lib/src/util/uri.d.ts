type URIContainer = {
    readonly uri: string;
};
export declare function makeFsUri(fsPath: string): string;
/**
 * Throw an exception if the URI is unparsable.
 */
/** @public KEEPING FOR TESTS */
export declare function validateUri<T extends URIContainer | string>(uri: T): T;
export declare function normalizeUri(uri: string): string;
/**
 * For a file system URI, returns the corresponding file system path. Otherwise
 * throws an error.
 */
export declare function fsPath(arg: URIContainer | string): string;
/**
 * For a file system URI, returns the corresponding file system path. Returns
 * undefined otherwise.
 */
export declare function getFsPath(uri: URIContainer | string): string | undefined;
/**
 * Ensure a file system URI has a file: scheme.  If it's not a file system URI, return undefined.
 */
export declare function getFsUri(uri: URIContainer | string): string | undefined;
/**
 * Joins together multiple path components, with a URI as the base.
 */
export declare function joinPath(uri: string, ...paths: string[]): string;
export declare function joinPath(uri: URIContainer, ...paths: string[]): URIContainer;
export declare function joinPath(uri: URIContainer | string, ...paths: string[]): URIContainer | string;
/**
 * Returns the base filename (no directory path) of a URI.
 */
export declare function basename(uri: URIContainer | string): string;
/**
 * Returns the directory name of a URI.
 * If the uri scheme is a notebook, will remove the fragment and change the scheme to file.
 */
export declare function dirname(uri: string): string;
export declare function dirname(uri: URIContainer): URIContainer;
export declare function dirname(uri: URIContainer | string): URIContainer | string;
export {};
//# sourceMappingURL=uri.d.ts.map