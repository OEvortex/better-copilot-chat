import { ICompletionsTextDocumentManagerService } from '../../textDocumentManager';
import { INeighborSource, NeighboringFileType, NeighborsCollection } from './neighborFiles';
export declare class OpenTabFiles implements INeighborSource {
    readonly docManager: ICompletionsTextDocumentManagerService;
    constructor(docManager: ICompletionsTextDocumentManagerService);
    private truncateDocs;
    /**
     * Get the neighbor files. Current it supports open editors.
     * @param uri The uri of the current open file.
     * @param languageId The language id of the current open file.
     * @param maxNumNeighborFiles The max number of neighbor files to return.
     * @returns Include 2 items.
     *          1. The merged unique documents, which is not exceeding MAX_NEIGHBOR_FILES.
     *          2. For each neighbor type, the files that are included in the merged unique documents.
     */
    getNeighborFiles(uri: string, languageId: string, maxNumNeighborFiles: number): Promise<{
        docs: NeighborsCollection;
        neighborSource: Map<NeighboringFileType, string[]>;
    }>;
}
//# sourceMappingURL=openTabFiles.d.ts.map