import { ServicesAccessor } from '../../../../../util/vs/platform/instantiation/common/instantiation';
import { CopilotToken } from './auth/copilotTokenManager';
type ServiceEndpoints = {
    proxy: string;
    'origin-tracker': string;
};
export declare function getEndpointUrl(accessor: ServicesAccessor, token: CopilotToken, endpoint: keyof ServiceEndpoints, ...paths: string[]): string;
/**
 * Return the endpoints from the most recent token, or fall back to the defaults if we don't have one.
 * Generally you should be using token.endpoints or getEndpointUrl() instead.
 */
export declare function getLastKnownEndpoints(accessor: ServicesAccessor): {
    api: string;
    telemetry: string;
    proxy: string;
    'origin-tracker'?: string;
} | ServiceEndpoints;
export {};
//# sourceMappingURL=networkConfiguration.d.ts.map