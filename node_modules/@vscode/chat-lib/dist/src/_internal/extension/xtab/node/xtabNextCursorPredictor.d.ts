import { IConfigurationService } from '../../../platform/configuration/common/configurationService';
import { NextCursorLinePrediction } from '../../../platform/inlineEdits/common/dataTypes/nextCursorLinePrediction';
import { IExperimentationService } from '../../../platform/telemetry/common/nullExperimentationService';
import { Result } from '../../../util/common/result';
import { ITracer } from '../../../util/common/tracing';
import { IInstantiationService } from '../../../util/vs/platform/instantiation/common/instantiation';
import { PromptPieces } from '../common/promptCrafting';
export declare class XtabNextCursorPredictor {
    private readonly computeTokens;
    private readonly instaService;
    private readonly configService;
    private readonly expService;
    private isDisabled;
    constructor(computeTokens: (text: string) => number, instaService: IInstantiationService, configService: IConfigurationService, expService: IExperimentationService);
    determineEnablement(): NextCursorLinePrediction | undefined;
    predictNextCursorPosition(promptPieces: PromptPieces, parentTracer: ITracer): Promise<Result</* zero-based line number */ number, Error>>;
}
//# sourceMappingURL=xtabNextCursorPredictor.d.ts.map