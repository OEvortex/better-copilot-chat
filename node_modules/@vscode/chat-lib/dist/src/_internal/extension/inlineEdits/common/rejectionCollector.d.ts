import { DocumentId } from '../../../platform/inlineEdits/common/dataTypes/documentId';
import { ObservableWorkspace } from '../../../platform/inlineEdits/common/observableWorkspace';
import { Disposable } from '../../../util/vs/base/common/lifecycle';
import { StringReplacement } from '../../../util/vs/editor/common/core/edits/stringEdit';
export declare class RejectionCollector extends Disposable {
    readonly workspace: ObservableWorkspace;
    private readonly _garbageCollector;
    private readonly _documentCaches;
    private readonly _tracer;
    constructor(workspace: ObservableWorkspace, trace: (s: string) => void);
    reject(docId: DocumentId, edit: StringReplacement): void;
    isRejected(docId: DocumentId, edit: StringReplacement): boolean;
    clear(): void;
}
//# sourceMappingURL=rejectionCollector.d.ts.map