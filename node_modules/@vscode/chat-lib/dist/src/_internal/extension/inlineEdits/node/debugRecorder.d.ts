import { DebugRecorderBookmark } from '../../../platform/inlineEdits/common/debugRecorderBookmark';
import { ObservableWorkspace } from '../../../platform/inlineEdits/common/observableWorkspace';
import { now } from '../../../platform/inlineEdits/common/utils/utils';
import { LogEntry } from '../../../platform/workspaceRecorder/common/workspaceLog';
import { Disposable } from '../../../util/vs/base/common/lifecycle';
export declare class DebugRecorder extends Disposable {
    private readonly _workspace;
    private readonly getNow;
    private _id;
    private readonly _documentHistories;
    private _workspaceRoot;
    constructor(_workspace: ObservableWorkspace, getNow?: typeof now);
    private _lastTimestamp;
    getTimestamp(): number;
    getRecentLog(bookmark?: DebugRecorderBookmark | undefined): LogEntry[] | undefined;
    createBookmark(): DebugRecorderBookmark;
}
//# sourceMappingURL=debugRecorder.d.ts.map