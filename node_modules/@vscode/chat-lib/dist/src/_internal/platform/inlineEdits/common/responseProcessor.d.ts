import { AsyncIterableObject } from '../../../util/vs/base/common/async';
import { LineReplacement } from '../../../util/vs/editor/common/core/edits/lineEdit';
export declare namespace ResponseProcessor {
    type DiffParams = {
        /**
         * Whether to emit a fast cursor line change event.
         * Defaults to false.
         */
        readonly emitFastCursorLineChange: boolean;
        readonly nSignificantLinesToConverge: number;
        readonly nLinesToConverge: number;
    };
    const DEFAULT_DIFF_PARAMS: DiffParams;
    /**
     *
     * @param originalLines
     * @param modifiedLines
     * @param cursorOriginalLinesOffset offset of cursor within original lines
     */
    function diff(originalLines: string[], modifiedLines: AsyncIterableObject<string>, cursorOriginalLinesOffset: number, params: DiffParams): AsyncIterable<LineReplacement>;
}
export declare class ArrayMap<K, V> {
    private map;
    /**
     * Appends a value to the array of values for the given key.
     */
    add(key: K, value: V): void;
    /**
     * Gets the array of values for the given key.
     * Returns an empty array if the key does not exist.
     */
    get(key: K): V[];
}
//# sourceMappingURL=responseProcessor.d.ts.map