import { OffsetRange } from '../../../../util/vs/editor/common/core/ranges/offsetRange';
import { StringText } from '../../../../util/vs/editor/common/core/text/abstractText';
import { DocumentId } from '../dataTypes/documentId';
import { Edits, RootedEdit } from '../dataTypes/edit';
import { LanguageId } from '../dataTypes/languageId';
export interface IHistoryContextProvider {
    /**
     * The result must contain a document with `docUri`.
    */
    getHistoryContext(id: DocumentId): HistoryContext | undefined;
}
export declare class HistoryContext {
    /**
     * Sorted by time, from least recent to most recent.
    */
    readonly documents: readonly DocumentHistory[];
    constructor(
    /**
     * Sorted by time, from least recent to most recent.
    */
    documents: readonly DocumentHistory[]);
    getMostRecentDocument(): DocumentHistory;
    getDocument(docId: DocumentId): DocumentHistory | undefined;
    getDocumentAndIdx(docId: DocumentId): {
        doc: DocumentHistory;
        idx: number;
    } | undefined;
}
export declare class DocumentHistory {
    readonly docId: DocumentId;
    readonly languageId: LanguageId;
    readonly base: StringText;
    readonly lastEdits: Edits;
    /**
     * Refers to the state after edits
    */
    readonly lastSelection: OffsetRange | undefined;
    readonly lastEdit: RootedEdit<import("../../../../util/vs/editor/common/core/edits/stringEdit").StringEdit>;
    constructor(docId: DocumentId, languageId: LanguageId, base: StringText, lastEdits: Edits, 
    /**
     * Refers to the state after edits
    */
    lastSelection: OffsetRange | undefined);
}
//# sourceMappingURL=historyContextProvider.d.ts.map