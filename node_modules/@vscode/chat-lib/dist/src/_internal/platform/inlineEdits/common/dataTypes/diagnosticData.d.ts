import { URI } from '../../../../util/vs/base/common/uri';
import { OffsetRange } from '../../../../util/vs/editor/common/core/ranges/offsetRange';
export declare class DiagnosticData {
    readonly documentUri: URI;
    readonly message: string;
    readonly severity: 'error' | 'warning';
    readonly range: OffsetRange;
    readonly code: string | number | undefined;
    readonly source: string | undefined;
    constructor(documentUri: URI, message: string, severity: 'error' | 'warning', range: OffsetRange, code: string | number | undefined, source: string | undefined);
    toString(): string;
    equals(other: DiagnosticData): boolean;
}
//# sourceMappingURL=diagnosticData.d.ts.map