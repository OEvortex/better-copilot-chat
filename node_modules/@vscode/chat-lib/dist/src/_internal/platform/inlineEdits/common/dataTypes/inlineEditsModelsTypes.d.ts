import { IValidator } from '../../../configuration/common/validator';
export declare namespace WireTypes {
    namespace Capabilities {
        type t = {
            promptStrategy: string;
        };
        function is(obj: unknown): obj is t;
        const validator: IValidator<t>;
    }
    namespace Model {
        type t = {
            serviceType: string;
            name: string;
            provider: string;
            capabilities: Capabilities.t;
        };
        const validator: IValidator<t>;
        function is(obj: unknown): obj is t;
    }
    namespace ModelList {
        type t = {
            models: Model.t[];
        };
        const validator: IValidator<t>;
        function is(obj: unknown): obj is t;
    }
}
//# sourceMappingURL=inlineEditsModelsTypes.d.ts.map