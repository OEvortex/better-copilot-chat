import { TextEdit } from '../../../../util/vs/editor/common/core/edits/textEdit';
import { Range } from '../../../../util/vs/editor/common/core/range';
import { TextLength } from '../../../../util/vs/editor/common/core/text/textLength';
export declare class TextLengthEdit {
    readonly edits: readonly SingleTextEditLength[];
    static readonly empty: TextLengthEdit;
    static fromTextEdit(textEdit: TextEdit): TextLengthEdit;
    private static _fromTextEditInfo;
    constructor(edits: readonly SingleTextEditLength[]);
    private _toTextEditInfo;
    compose(other: TextLengthEdit): TextLengthEdit;
    /**
     * Returns the range of the edit, or undefined if the edit is empty.
     */
    getRange(): Range | undefined;
    toString(): string;
}
export declare class SingleTextEditLength {
    readonly range: Range;
    readonly newLength: TextLength;
    constructor(range: Range, newLength: TextLength);
    toString(): string;
}
//# sourceMappingURL=textEditLength.d.ts.map