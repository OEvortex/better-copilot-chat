import type * as vscode from 'vscode';
import { StringEdit } from '../../../util/vs/editor/common/core/edits/stringEdit';
import { OffsetRange } from '../../../util/vs/editor/common/core/ranges/offsetRange';
import { Position, Range, TextEdit } from '../../../util/common/test/shims/vscodeTypesShim';
export declare class PositionOffsetTransformer {
    private readonly _eol;
    private _lines;
    private _lineStarts;
    constructor(text: string);
    getText(): string;
    applyOffsetEdits(offsetEdits: StringEdit): void;
    private _acceptDeleteRange;
    private _acceptInsertText;
    /**
     * All changes to a line's text go through this method
     */
    private _setLineText;
    getLineCount(): number;
    getOffset(position: Position): number;
    getPosition(offset: number): Position;
    toRange(offsetRange: OffsetRange): Range;
    toOffsetRange(range: Range): OffsetRange;
    toOffsetEdit(edits: readonly TextEdit[]): StringEdit;
    toTextEdits(edit: StringEdit): TextEdit[];
    validatePosition(position: vscode.Position): vscode.Position;
    validateRange(range: Range): Range;
}
//# sourceMappingURL=positionOffsetTransformer.d.ts.map